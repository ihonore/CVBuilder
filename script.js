function changeColor(){
    document.getElementById("your-name").style.color=
    'rgb('+Math.round(Math.random()*255)+','
    +Math.round(Math.random()*255)+','
    +Math.round(Math.random()*255)+')'
}